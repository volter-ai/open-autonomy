#!/usr/bin/env bun
import{readFileSync,writeFileSync}from'node:fs';import type{RuntimeLedgerCorpus}from'../packages/core/src/organization-runtime-ledger';const c=JSON.parse(readFileSync('docs/runtime-ledgers/r23-closure.json','utf8'))as RuntimeLedgerCorpus;c.evidenceLedger.push({id:'ev-r24-runtime',kind:'artifact',uri:'packages/core/src/organization-r24-twin-matched-engineering.ts',producer:'open-autonomy R24'},{id:'ev-r24-native',kind:'live-run',uri:'docs/evidence/R24-TWIN-MATCHED-ENGINEERING.json',producer:'pinned Hermes and Paperclip native campaign'},{id:'ev-r24-tests',kind:'test',uri:'packages/core/src/organization-r24-native-engineering-adapters.test.ts',producer:'Bun verifier'},{id:'ev-r24-review',kind:'review',uri:'docs/evidence/R24-TWIN-CLOSURE-SKEPTICAL-REVIEW.md',producer:'skeptical engineering review'});const ev=['ev-r24-runtime','ev-r24-native','ev-r24-tests','ev-r24-review'];for(const x of c.obligationLedger)if(x.checkpoint==='R24'){x.disposition='preserved';x.assurance='live-observed';x.evidence=ev;}for(const[a,b]of[['unchanged locked randomized binding and complete native outcomes','R24-EXP-1'],['portable outcome equality across actual native substrate processes','R24-REF-1'],['paired effect and uncertainty protocol with inconclusive outcomes representable','R24-STAT-1'],['terminal operational and semantic differences classified without exclusions','R24-FALS-1'],['model tool repository harness prompt context session isolation and credential locks','R24-LOCK-1']])c.semanticCoverageLedger.push({construct:a,checkpoint:'R24',disposition:'preserved',obligationIds:[b]});const s=c.checkpointStateLedger.find(x=>x.id==='R24');if(!s||s.status!=='ready')throw Error('unexpected R24 predecessor');s.status='complete';for(const x of c.checkpointStateLedger)if(x.status==='blocked'&&x.dependsOn.every(id=>c.checkpointStateLedger.find(y=>y.id===id)?.status==='complete'))x.status='ready';writeFileSync('docs/runtime-ledgers/r24-closure.json',JSON.stringify(c,null,2)+'\n');
