# Testbed Roadmap

This repository is the live proof bed for the root Open Autonomy roadmap.
Detailed product direction stays in the root `docs/ROADMAP.md`; this file only
records the local testbed role.

Use `docs/TEST_MATRIX.md` for scenarios and `docs/TEST_RUNS.md` for evidence.
