# R13 closure

R13 closes the A2A 0.3 and Oracle Agent Spec native interoperation checkpoint. The implementation preserves exact native task, message, artifact, part, card, endpoint, flow, component, behavior, and authorization payloads; produces explicit refinement losses where a portable projection is not exact; binds tenant, organization, actor, task, and context identities; and rejects ambiguous, malformed, unsafe, or non-global endpoints.

Closure is supported by the implementation, its 16-test adversarial suite, and the independent skeptical review. The closure ledger marks all R13 obligations property-tested and records total semantic coverage without adding A2A- or Agent-Spec-specific vocabulary to canonical Organization IR.
