import{expect,test}from"bun:test";import{assertAcceptedGitHubSearch,censusTupleDigest,leafCountDigest,selectOutDomainReview,splitCreationRange,splitStarRange}from"./organization-universality-census";
test("U1 census partitions are disjoint, exhaustive, deterministic, and terminal",()=>{expect(splitStarRange([1000,1001])).toEqual([[1000,1000],[1001,1001]]);expect(splitStarRange([1000,1004])).toEqual([[1000,1002],[1003,1004]]);expect(splitCreationRange(["2026-01-01","2026-01-02"])).toEqual([["2026-01-01","2026-01-01"],["2026-01-02","2026-01-02"]]);expect(()=>splitCreationRange(["2026-01-01","2026-01-01"])).toThrow("unpartitionable")});
test("U1 accepts only the frozen GitHub response shape",()=>{const value={total_count:1,incomplete_results:false,items:[{node_id:"n",full_name:"o/r",stargazers_count:1000,fork:false,archived:false,default_branch:"main"}]};expect(assertAcceptedGitHubSearch(value)).toEqual(value);for(const bad of [{...value,total_count:-1},{...value,incomplete_results:"false"},{...value,items:[{...value.items[0],node_id:null}]}])expect(()=>assertAcceptedGitHubSearch(bad)).toThrow("malformed")});
test("U1 convergence digests are canonical and semantic",()=>{const a=[{node_id:"b",full_name:"b/r",stargazers_count:2,fork:false,archived:false},{node_id:"a",full_name:"a/r",stargazers_count:1,fork:true,archived:false}];expect(censusTupleDigest(a)).toBe(censusTupleDigest([...a].reverse()));expect(censusTupleDigest(a)).not.toBe(censusTupleDigest([{...a[0]!,stargazers_count:3},a[1]!]));expect(leafCountDigest([["b",2],["a",1]])).toBe(leafCountDigest([["a",1],["b",2]]))});
test("U1 out-domain audit selects exactly ceil(N/100) by domain-separated SHA-256",()=>{expect(selectOutDomainReview([])).toEqual([]);expect(selectOutDomainReview(["only"])).toEqual(["only"]);const ids=Array.from({length:101},(_,i)=>`node-${i}`),selected=selectOutDomainReview(ids);expect(selected).toHaveLength(2);expect(selectOutDomainReview([...ids].reverse())).toEqual(selected);expect(()=>selectOutDomainReview(["x","x"])).toThrow("duplicate")});
