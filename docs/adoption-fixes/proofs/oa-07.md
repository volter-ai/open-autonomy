# OA-07 proof — AC-7/AC-8 ("PM day-one backlog fence") "live-pending" gaps, closed

Spec: `docs/adoption-fixes/OA-07-day-one-backlog-fence-install-paused.md`. OA-07 landed
code-complete for the primary mechanism (AC-1..6, AC-9: the `.open-autonomy/paused` marker,
seed-once semantics, the scheduler gate, the launch-seam refusal) and for the secondary
policy vocabulary (AC-6's `policy.dispatch` box in `profiles/simple-sdlc/ir.yml`). Two
acceptance criteria on the secondary mechanism's **PM doctrine** — the prose rules in
`profiles/simple-sdlc/skills/pm/SKILL.md` that consume that policy — were left
**live-pending**:

- **AC-7** (allowlist enforced): "Live tick test (unpaused, `mode: allowlist`): board has
  one `ready` issue without `oa-approved` and one with it. One tick → the PM dispatches
  only the labeled issue; tick output names the unlabeled one as fenced."
- **AC-8** (body read + deferral respected): "Live tick test: one `ready`, `oa-approved`
  issue whose body contains 'Do not dispatch — deferred per decision record X.' One tick →
  no develop launched for it; tick output reports it blocked-for-human. Repeat tick → still
  not dispatched (no per-tick re-pick)."

This proof closes the **mechanism half** of both gaps as **committed, deterministic
tests** — modeled directly on OA-08's own AC-7 closure (`docs/adoption-fixes/proofs/oa-08.md`,
`packages/substrate-local/src/pm-escalation.test.ts`): a real compiled install, a real
`ztrack` board, a real `.open-autonomy/autonomy.yml` carrying `policy.dispatch`, and a
**canned PM driver** that replays the doctrine's exact policy read / board read / body read /
`bun scripts/runner.ts launch develop` dispatch — zero model calls, zero live process, and
(the whole point) durable: it lives in git, not in `/workspace/proofs/`.

## 1. Diff summary

Commits on `build/oa-07` (branched from `adoption-fixes-backlog @ 1b1a690`):

| Commit | What |
|---|---|
| `1ef6cc1` | Adds `packages/substrate-local/src/pm-dispatch-fence.test.ts` (new) + `packages/substrate-local/src/test-support/canned-pm-dispatch-fence.ts` (new). No product code changed — `profiles/simple-sdlc/skills/pm/SKILL.md`'s doctrine, `profiles/simple-sdlc/ir.yml`'s `policy.dispatch` box, the pause marker, the scheduler gate, and `runner-frontend.ts`'s launch-seam refusal (all already landed by OA-07) are untouched. |
| `2103416` | First cut of `docs/adoption-fixes/proofs/oa-07.md` — committed BEFORE the AC-7 fixture fix below, so it can't be wiped, at the cost of describing the pre-fix (tautological) fixture. |
| `9451eb1` | AC-7 fixture fix (skeptical-reviewer blocker, landed AFTER `2103416`'s proof doc): re-orders the fixture so the un-opted-in issue is the board's TOP candidate, making the allowlist gate load-bearing on the real dispatch outcome (not just the driver's self-reported `fenced` note) + an explicit board-order assertion; plus coupled NITs (hyphenated deferral markers, fuller jsdoc precondition disclosure). AC-8 and the driver's core structure unchanged. |
| `ef92f05` | Updates `docs/adoption-fixes/proofs/oa-07.md` to describe the fixed (non-tautological) fixture from `9451eb1`, superseding `2103416`'s pre-fix text. |
| (this doc's own commit, branch `build/oa-07-ci-fix` off `adoption-fixes-backlog @ dd748c9`) | **Governed-CI fix**: `9451eb1`'s own "fix" turned out to still be wrong — it made AC-7 assert `readBoard(dir)[0] === unlabeledId`, i.e. it hard-coded a specific `ztrack issue list` scan order as a *precondition*. That assumption (`ztrack` lists "most-recently-touched first") is FALSE: root-caused by reading ztrack's own source, the markdown issue-per-file backend's `mdIds()` returns `readdirSync(dir)` **unsorted** — filesystem/environment-dependent, with no relationship to touch recency. A clean-checkout governed-CI runner returned the fixture's two issues in the OPPOSITE order from the authoring dev box, failing the ordering precondition (`expect(boardIds[0]).toBe(unlabeledId)` → got `LOCAL-1`, wanted `LOCAL-2`) even though the fence itself was never broken. This commit removes that fragile ordering precondition (confirming it was never load-bearing: the fence's pass 1 is unconditional over the whole `ready` set, so `tick.fenced`/`tick.dispatched` and the sessions-file/worktree assertions already held regardless of scan order — verified by temporarily patching `readBoard` to return the board reversed and re-running green) and replaces the ordering-dependent non-tautology argument with a new, **order-independent** committed positive control: a "CONTRAST" test (single-issue board — no relative ordering to depend on) that gives the exact same unlabeled-shaped issue the `oa-approved` label and shows it gets dispatched, proving the original exclusion was caused by the fence, not by anything else about the issue. Neither the expected value nor the non-tautology protection was weakened — the fragile *precondition* was removed, the *outcome* assertions and the non-tautology guarantee were kept (and strengthened: they no longer depend on an unverified ordering claim). |

> Provenance note: the `build/oa-07` SHAs above are the original build-branch commits; the identical
> content landed on `adoption-fixes-backlog` as a two-commit lineage, both ancestors of the branch
> HEAD. The initial add `38cdb47` ("OA-07 AC-7/AC-8: close the day-one backlog-fence PM-doctrine gaps
> with committed deterministic tests") carried the PRE-FIX, tautological fixture — it is byte-identical
> to build-branch `2103416`, the version this doc explicitly supersedes, NOT the fixture documented
> here. The fixture fix `95aeb05` ("OA-07 AC-7: fix tautological fixture") is byte-identical to
> build-branch `9451eb1` — the non-tautological fixture (readBoard import, top-candidate re-order,
> board-order assertions) that THIS DOC DESCRIBES — and its `pm-dispatch-fence.test.ts` matches the
> branch HEAD byte-for-byte. So `95aeb05`, not `38cdb47`, is the durable ancestor carrying the
> documented test content; the build-branch SHAs are retained only as the original authoring reference.

Net test-infrastructure diff from the baseline to the CI-fix tip, the two files this
proof owns (no other files touched by this fix):

```
$ git diff --stat dd748c9..HEAD -- packages/substrate-local/src/pm-dispatch-fence.test.ts packages/substrate-local/src/test-support/canned-pm-dispatch-fence.ts
 packages/substrate-local/src/pm-dispatch-fence.test.ts                | 83 +++++++++++++++++-----
 packages/substrate-local/src/test-support/canned-pm-dispatch-fence.ts | 17 +++--
 2 files changed, 76 insertions(+), 24 deletions(-)
```

The diff removes the ordering precondition (`expect(boardIds[0]).toBe(unlabeledId)`) and its
now-disproven "most-recently-touched" reasoning, adds a new order-independent "OA-07 AC-7
CONTRAST" describe/test block (a single-issue board — no relative ordering to depend on), and
rewrites two comment blocks (in the test file and in `readBoard`'s jsdoc) to state the confirmed
root cause of ztrack's list order instead of the disproven claim.

Neither `packages/substrate-local/src/launch-verification.test.ts` (OA-08/other-builder
owned) nor any OA-08 file (`pm-escalation.test.ts`,
`test-support/canned-pm-escalation.ts`) is touched or imported from — this driver owns a
small private duplicate of the generic ztrack-CLI helpers it needs (`resolveZtrackCli`,
`ztrackOk`, board/body reads), mirroring OA-08's own precedent of duplicating
`launch-verification.test.ts`'s `installStubTermfleet` rather than reaching across a
sibling builder's file. The one thing genuinely **reused** (not forked) is the shared
model-free stub termfleet provider, `packages/substrate-local/src/test-support/stub-termfleet.ts`
— imported directly, per this task's own instruction. (Note: OA-08's own
`pm-escalation.test.ts` still carries its *own inline* duplicate of the stub, predating the
shared module; only `launch-verification.test.ts` and this OA-07 suite import the shared
`stub-termfleet.ts`. OA-07's import IS the shared one — the accurate claim is "this suite
reuses the shared module", not "every PM-doctrine suite already does".)

## 2. Test command(s) + pasted output

Updated after the governed-CI fix (branch `build/oa-07-ci-fix`): the suite now has THREE
tests (AC-7's main test, the new order-independent AC-7 CONTRAST positive control, and
AC-8), each with a generous 30s timeout — all drive real subprocesses: git init/commit,
`ztrack init` + `issue create`/`issue edit`, and a REAL `bun scripts/runner.ts launch
develop` (creates a git worktree and, for every eligible issue, a real (stub-provider)
termfleet session):

```
$ bun test packages/substrate-local/src/pm-dispatch-fence.test.ts
bun test v1.3.11 (af24e281)

 3 pass
 0 fail
 33 expect() calls
Ran 3 tests across 1 file. [10.34s]
```

Re-run 10 times back to back to confirm it isn't flaky (all 10 identical: 3 pass / 0 fail
/ 33 expect() calls):

```
$ for i in $(seq 1 10); do bun test packages/substrate-local/src/pm-dispatch-fence.test.ts 2>&1 | tail -6; done
... (×10, each: "3 pass / 0 fail / 33 expect() calls / Ran 3 tests across 1 file")
```

**Order-independence proof** (the CI-robustness requirement this fix exists to satisfy):
`readBoard()` in `canned-pm-dispatch-fence.ts` was temporarily patched to `.reverse()` its
result (a throwaway one-line change, reverted immediately after), and the suite re-run —
confirming the outcome is invariant to board scan order, not merely "happens to pass on
this machine":

```
$ # throwaway patch: readBoard() → (JSON.parse(out) as ZtrackListRow[]).reverse()
$ bun test packages/substrate-local/src/pm-dispatch-fence.test.ts   # REVERSED order
bun test v1.3.11 (af24e281)

 3 pass
 0 fail
 33 expect() calls
Ran 3 tests across 1 file. [15.32s]
$ # patch reverted (diff against pre-patch backup: byte-identical)
$ bun test packages/substrate-local/src/pm-dispatch-fence.test.ts   # UNREVERSED (normal) order
bun test v1.3.11 (af24e281)

 3 pass
 0 fail
 33 expect() calls
Ran 3 tests across 1 file. [12.02s]
```

Both orderings pass identically — the mixed-board AC-7 assertions never depended on which
of the two simultaneously-`ready` issues the board lists first (pass 1's allowlist gate is
unconditional over the whole `ready` set), and the new CONTRAST/AC-8 tests are
single-issue boards with no relative ordering to depend on in the first place.

The whole `substrate-local` package (13 files at this branch's base commit, including
OA-08's own `pm-escalation.test.ts` and `launch-verification.test.ts` — confirms no
interference):

```
$ bun test packages/substrate-local
bun test v1.3.11 (af24e281)

 96 pass
 0 fail
 356 expect() calls
Ran 96 tests across 13 files. [51.52s]
```

Full repo gate (`bun run check` — tsc, every package's tests including this one,
conformance, runtime-sync, compile, profiles, policy-consumers, doc-vars, preflight,
doctor, dogfood, provision, supply-chain, public-agent, agent-proxy, proof,
soc2-register, release-consistency, pack-smoke):

```
$ bun run check
...
$ bun test packages/*/src/*.test.ts
bun test v1.3.11 (af24e281)

 197 pass
 1 todo
 0 fail
 562 expect() calls
Ran 198 tests across 20 files. [29.74s]
...
pack-smoke: ok — doctor --json — self: PASS, verdict: FAIL

pack-smoke: all checks passed ✓
$ echo "EXIT_CODE:${PIPESTATUS[0]}"
EXIT_CODE:0
```

Exit code 0, full run, nothing skipped. Counts are higher than the original `9451eb1`-era
snapshot (`19 files` / `192 tests`) because other builders' work has landed on
`adoption-fixes-backlog` since; this CI fix itself is net **one additional test** (the new
CONTRAST positive control) plus zero net product-code lines.

## 3. Per-AC assertions, and how to reproduce

Fixture (`scaffold()` in `pm-dispatch-fence.test.ts`): a real `compileLocal(ir)` output (pm
+ develop agents, `codeHost: 'github'`, `policy.box.dispatch: { mode: 'allowlist',
allow_label: 'oa-approved' }` — mirrors `profiles/simple-sdlc/ir.yml`'s own landed policy
box), written to a fresh `mkdtemp` dir, the shared stub termfleet installed, a REAL `ztrack
init --preset simple-sdlc`, then `git init` + `git add -A` + `git commit` — a real git repo
with a real committed ztrack board. Unlike OA-08's AC-7 fixture, the `develop` skill is
**left intact** here (a stub `SKILL.md`, same as every other agent's copy) — this suite is
about *which* issues get dispatched, not whether a dispatch can succeed at all, so a
successful dispatch must be observable.

Each AC calls `runDispatchFenceTick()` (`test-support/canned-pm-dispatch-fence.ts`), which:
- reads `policy.dispatch` from a REAL `.open-autonomy/autonomy.yml` (parsed off disk, never
  cached);
- reads the board via a REAL `ztrack issue list --state open --limit 100 --json
  identifier,title,state,labels,assignee` (pm/SKILL.md:58's own command);
- evaluates the allowlist gate over the **whole** `ready` set first (pm/SKILL.md:18-23 is a
  blanket statement — "regardless of its `ready` state" — true independent of board-list
  position; see the driver's own jsdoc for why this is a deliberate two-pass design, not an
  arbitrary implementation choice: ztrack's list order was empirically observed to reorder
  on label/body edits, e.g. most-recently-touched first, so relying on scan order for the
  allowlist check would make the test's pass/fail depend on incidental ordering);
- for every allowlist-cleared candidate, in board-list order, reads its body via a REAL
  `ztrack issue view <id> --json` and scans for a deferral marker (pm/SKILL.md:85-93);
- dispatches the REAL `bun scripts/runner.ts launch develop --ref <id> --branch
  agent/issue-<id>` for the first candidate that clears both gates, and stops (one
  dispatch per tick, per pm/SKILL.md:134).

**AC-7** (`pm-dispatch-fence.test.ts`, "OA-07 AC-7" describe block):
- Board: the approved issue is created AND labeled `oa-approved` (a REAL `ztrack issue edit
  --add-label oa-approved`); a second, unlabeled `ready` issue is created. **Board scan
  order is deliberately NOT asserted on or relied upon** — see the box below for why an
  earlier revision of this fixture did rely on it, and why that was wrong.
- `tick.fenced` === `[{ id: <unlabeled>, note: 'fenced (no oa-approved)' }]` — the exact
  token shape pm/SKILL.md:21 specifies. This holds regardless of scan order: pass 1 (the
  allowlist gate) is evaluated over the **whole** `ready` set unconditionally
  (`canned-pm-dispatch-fence.ts`'s own pass-1 jsdoc), not by scanning until some "top"
  candidate is found.
- `tick.blockedForHuman` === `[]` — a fenced issue is explicitly NOT reported
  blocked-for-human (pm/SKILL.md:21-22: "it's not a decision, it's simply not yet opted
  in").
- `tick.dispatched` === `[<approved>]`; `tick.launch.status === 0` — the REAL runner
  accepted this dispatch. Since the unlabeled issue is unconditionally excluded from pass 2
  no matter where it sits in the scan, the approved issue is the only candidate pass 2 ever
  reaches, and it's the one that gets launched — deterministically, on any board ordering.
- Independent proof (not the driver's own return value): the shared stub provider's
  `OA_STUB_TF_SESSIONS_FILE` contains **exactly one** session record, `agent: 'develop'`,
  whose `cwd` contains `agent-issue-<approved>` and does **not** contain
  `agent-issue-<unlabeled>` — attribution proven by inspecting the sessions file, not
  merely trusting the tick output.
- The fenced issue never got a worktree or branch at all (`git rev-parse --verify
  agent/issue-<unlabeled>` fails; the `.worktrees/agent-issue-<unlabeled>` directory does
  not exist) — it was never launched, full stop.

> **Box: why board order is no longer asserted on (the governed-CI incident).** An earlier
> revision of this fixture (`9451eb1`) made the non-tautology argument depend on ztrack's
> scan order: it created the approved issue first, the unlabeled issue last, asserted
> `readBoard(dir)[0] === <unlabeled>` ("ztrack lists most-recently-touched first, so the
> unlabeled issue is the board's top candidate"), and argued that this made the fence
> load-bearing (without it, the top/unlabeled candidate would be dispatched first). That
> assumption was **false** — root-caused by reading ztrack's own source
> (`node_modules/ztrack/dist/cli.js`'s `mdIds()`): the markdown issue-per-file backend lists
> issues in raw, **unsorted** `readdirSync(dir)` order, which is filesystem/environment-
> dependent and has no guaranteed relationship to creation or touch recency. A governed-CI
> runner on a clean checkout returned the two issues in the OPPOSITE order from the
> authoring dev box, failing the `boardIds[0] === unlabeledId` assertion — even though the
> fence itself was never broken (`tick.fenced`/`tick.dispatched` were still correct on that
> run; only the incidental ordering precondition failed). The fix (this doc's own commit)
> removes that precondition — it was never load-bearing, since pass 1 is unconditional over
> the whole `ready` set — and moves the non-tautology argument to an
> order-independent committed positive control (below), instead of an unverifiable ordering
> claim.

- **Non-tautological, order-independently**: see the new **"OA-07 AC-7 CONTRAST"** describe
  block, immediately below the main AC-7 test in the same file. On a **single-issue board**
  (no relative ordering between two issues to depend on), it takes the exact same
  unlabeled-shaped issue and gives it ONLY the `oa-approved` label — nothing else about the
  issue changes — and shows it gets dispatched (`tick.dispatched === [id]`, one session
  lands in its worktree). This proves the issue was always a genuine, otherwise-dispatchable
  candidate, so its exclusion in the main AC-7 test above is caused BY the fence, not by
  some other property of the fixture (mirrors the OA-09 "CONTRAST (proves the decoy is real,
  not vacuous)" pattern in `provider-landing.test.ts`). Combined with the main test's
  order-independent `tick.fenced`/`tick.dispatched` assertions, together these prove: the
  fence excludes an otherwise-dispatchable issue — deterministically AND non-tautologically
  — as the §4 probe below re-demonstrates by literal removal.

**AC-8** (`pm-dispatch-fence.test.ts`, "OA-07 AC-8" describe block):
- Board: one `ready`, `oa-approved` issue whose body reads (in full) "Do not dispatch —
  deferred per decision record OA-DR-12." plus surrounding context — the spec's own example
  marker shape.
- **Tick 1**: `tick1.fenced === []` (it has the label — never a fence hit);
  `tick1.blockedForHuman === [{ id: <deferred>, note: 'blocked-for-human (body: explicit
  deferral marker)' }]`; `tick1.dispatched === []`; `tick1.launch === null` — **no
  subprocess is spawned at all**, not merely one that fails. The stub provider's sessions
  file has zero records; no worktree/branch was created for the issue.
- **Tick 2** (a fresh call to `runDispatchFenceTick`, re-reading the board and the body from
  scratch — no in-memory state carried over, matching "there is no separate 'already saw
  this' memory to maintain"): identical result — `blockedForHuman` still names the same
  issue, `dispatched` still `[]`, `launch` still `null`, sessions file still empty. This is
  the AC's own "repeat tick → still not dispatched" requirement, proven by literally calling
  the tick function twice and asserting the same outcome both times.
- **Non-tautological**: the issue is `ready`, `oa-approved` (clears the allowlist cleanly),
  and has no branch — with the body-read removed it WOULD be dispatched (again, verified
  live — see §4).

**Reproduce:**
```
cd packages/substrate-local
bun test src/pm-dispatch-fence.test.ts
```

## 4. Non-tautology probe (both guards independently disabled and restored)

Per this task's own verification requirement, each gate was temporarily neutralized in
`test-support/canned-pm-dispatch-fence.ts`, the affected test(s) re-run to confirm they FAIL
for the expected reason, then the guard was restored and all tests re-verified green. This
section was re-run after the governed-CI fix to confirm the probe still bites — order
independently this time.

**Probe 1 — neutralize the allowlist fence** (commented out the fence `continue` at
`canned-pm-dispatch-fence.ts:198` — the `fenced` note is still recorded, but the fenced
issue is no longer excluded from the pass-2 dispatch scan; exactly the transcription-error
class this test exists to catch), then ran the `-t "AC-7"` filter (both the main AC-7 test
and the new CONTRAST positive control match this filter).

The main AC-7 test fails on the **dispatch-outcome** assertion (`tick.dispatched`) — with
the fence disabled, whichever issue ztrack's (unsorted, environment-dependent) scan happened
to reach first in pass 2 this run got dispatched instead of the approved one (this run:
the unlabeled issue, `LOCAL-2`, was reached first and dispatched in place of the approved
`LOCAL-1`) — while the CONTRAST test (a single-issue board, unaffected by this mutant since
its one issue already carries the label) still passes:

```
$ bun test packages/substrate-local/src/pm-dispatch-fence.test.ts -t "AC-7"
226 |     expect(tick.dispatched).toEqual([approvedId]);
                                  ^
error: expect(received).toEqual(expected)

  [
-   "LOCAL-1",
+   "LOCAL-2",
  ]

(fail) OA-07 AC-7: allowlist fence enforced over the REAL board + REAL launch dispatch >
one unlabeled ready issue is fenced; the oa-approved one is dispatched, and ONLY it gets a
session [1795.78ms]

 1 pass
 1 filtered out
 1 fail
 11 expect() calls
Ran 2 tests across 1 file. [3.43s]
```

This is the whole point of the fixture: with the fence removed, the un-opted-in issue is
genuinely dispatchable and, on at least one board ordering, actually IS dispatched in
preference to the approved one — exactly the F-7 audit failure (a pre-existing item worked
before the operator opted it in) OA-07 exists to prevent. Crucially, this probe bites
**regardless of which of the two issues the board scans first** — the mutant breaks the
test whichever issue pass 2 reaches, because with the fence gone, pass 2 dispatches the
FIRST eligible candidate in scan order, period. Confirms AC-7's fixture is non-tautological
and this property no longer depends on a specific (and, per the CI incident, unverifiable)
scan order.

**Probe 2 — restore probe 1, then disable the body-read gate** (changed `if
(DEFERRAL_MARKER.test(body))` to `if (false && DEFERRAL_MARKER.test(body))`), ran AC-8
alone:

```
$ bun test packages/substrate-local/src/pm-dispatch-fence.test.ts -t "AC-8"
error: expect(received).toEqual(expected)

- [
-   {
-     "id": "LOCAL-1",
-     "note": "blocked-for-human (body: explicit deferral marker)",
-   },
- ]
+ []

(fail) OA-07 AC-8: body-read defers dispatch, and the doctrine re-reads the body every
tick > a ready+oa-approved issue whose body says "do not dispatch" gets NO session, on
tick 1 AND tick 2 [2267.47ms]

 0 pass
 1 filtered out
 1 fail
```

With the body-read removed, the deferred issue is dispatched on tick 1 (never even
evaluated for its explicit "do not dispatch" text) — exactly the TF-603 audit failure
OA-07 exists to prevent. Confirms AC-8's fixture is non-tautological: the body-read is
load-bearing for the test to pass.

**Restore + re-verify** (each probe was applied to a working copy of the driver/test, then
`diff`ed against a pre-probe backup to confirm a byte-identical restore before re-running):

```
$ diff /tmp/canned-pm-dispatch-fence.ts.bak2 packages/substrate-local/src/test-support/canned-pm-dispatch-fence.ts
(no output — byte-identical)
$ diff /tmp/test.bak2 packages/substrate-local/src/pm-dispatch-fence.test.ts
(no output — byte-identical)
$ bun test packages/substrate-local/src/pm-dispatch-fence.test.ts
bun test v1.3.11 (af24e281)

 3 pass
 0 fail
 33 expect() calls
Ran 3 tests across 1 file. [4.30s]
```

## 5. HONEST BOUNDARY

This proves the **mechanism half** of both ACs — the REAL `policy.dispatch` read, the REAL
board read, the REAL per-issue `ztrack issue view` body read, and the REAL `bun
scripts/runner.ts launch develop` dispatch landing a session for exactly the eligible
issue (proven by the stub provider's own session file, not just the driver's return value)
— deterministically and durably (it survives a full repo wipe: nothing here depends on a
live process, termfleet, or any external service; the only non-builtin dependency is the
repo's own pinned `ztrack@1.0.0` devDependency).

It does **NOT** prove that a real-model PM, handed only `SKILL.md` and the board each fresh
tick, independently **reads** the doctrine and **decides** to take these exact actions —
in particular, a model's own reading comprehension of an arbitrary prose deferral marker
("Do not dispatch", "blocked by decision X", a paraphrase neither test author anticipated,
…) is inherently a live, model-judgment question that no fixed regex or canned driver can
stand in for. That judgment is canned in
`test-support/canned-pm-dispatch-fence.ts`'s `runDispatchFenceTick` — its function body is
a direct, literal transcription of pm/SKILL.md's allowlist-gate and body-read rules
(:18-23,79-80,85-93), not independently derived by a model. This is consistent with:
- the spec's own AC-7/AC-8 text ("Live tick test" — judgment-layer, verified behaviorally,
  never claimed as unit-testable);
- OA-08's own AC-7 honest boundary (`docs/adoption-fixes/proofs/oa-08.md` §4), which draws
  the identical mechanism-vs-judgment line for the sibling escalation doctrine;
- `CLAUDE.md`'s "Built vs designed" section, which names the sibling case (escalate-on-SLA)
  as a live-proven `test.todo`, never a unit test.

**Named residual gap:** a live/bench run of the ACTUAL `pm` skill (model-driven), on a
populated board carrying both a fenced and an approved issue (AC-7) and separately on a
board carrying a body-deferred issue re-ticked twice (AC-8), reproducing these exact
outcomes — fence the unlabeled one, dispatch only the approved one; refuse the deferred one
on tick 1 AND tick 2 — remains the only proof of the judgment layer itself. This file is
deliberately **not** that proof and does not claim to be. It closes the mechanical half of
both ACs (the half that doesn't need a model to be true) as a committed, repeatable,
durable test; the judgment half stays exactly where the spec always said it belonged — live
verification.

## 6. Attestation — files touched, commits

Files touched (branch `build/oa-07`, isolated worktree at `/workspace/wt-oa07`, no other
builder's files touched):
- `packages/substrate-local/src/pm-dispatch-fence.test.ts` (new) — the AC-7 + AC-8 tests +
  their header comment (doctrine quoted in full, honest-boundary statement duplicated there
  for readers who never open this doc).
- `packages/substrate-local/src/test-support/canned-pm-dispatch-fence.ts` (new) — the
  canned PM driver (`resolveZtrackCli`, `ztrack`/`ztrackOk`, `readBoard`, `readIssueBody`,
  `readDispatchPolicy`, `runDispatchFenceTick`).
- `docs/adoption-fixes/proofs/oa-07.md` (this file, new).

Explicitly NOT touched (guardrails):
- `packages/substrate-local/src/launch-verification.test.ts` (another builder's file).
- Any OA-08 file (`pm-escalation.test.ts`, `test-support/canned-pm-escalation.ts`) — read
  as the modeling template, never edited or imported from.
- No product code anywhere (`profiles/simple-sdlc/skills/pm/SKILL.md`,
  `profiles/simple-sdlc/ir.yml`, `packages/substrate-local/src/emit.ts`,
  `packages/substrate-local/src/runner-frontend.ts` — all read-only in this task).
- `adoption-fixes-backlog` and `main` (this work lives only on `build/oa-07`).

Commits (branch `build/oa-07`, pushed to `origin/build/oa-07` after each):
- `1ef6cc1` — OA-07 AC-7/AC-8: close the day-one backlog-fence PM-doctrine gaps with
  committed deterministic tests.
- `9451eb1` — OA-07 AC-7: fix tautological fixture (make the unlabeled issue the board's top
  candidate so the gate is load-bearing on the real dispatch outcome, not just the self-report)
  + coupled NITs (hyphenated deferral markers, fuller jsdoc precondition disclosure). Fixes a
  blocker the skeptical reviewer found. AC-8 unchanged.
- (this doc's own commit — see `git log docs/adoption-fixes/proofs/oa-07.md`).

Ztrack board mechanics: **REAL**, not simulated. Every `ztrack issue create` /
`issue edit --add-label` / `issue list` / `issue view` call in this proof is a genuine
invocation of the repo's own pinned `ztrack@1.0.0` CLI (resolved by absolute path from this
package's own `node_modules`) against a genuine `simple-sdlc`-preset board. The stub
termfleet provider is the SHARED module
(`packages/substrate-local/src/test-support/stub-termfleet.ts`) — imported, not forked — so
this suite adds zero model-calling surface. (Its no-live-model safety posture matches the
other PM-doctrine suites in intent; the shared module is imported here and by
`launch-verification.test.ts`, while OA-08's `pm-escalation.test.ts` reaches the same
model-free guarantee via its own inline duplicate of the stub.)

Verified green at HEAD (`9451eb1`, code) and that revision's proof-doc commit: `bun run
check` (full repo gate) exited 0; `bun test packages/substrate-local/src/pm-dispatch-fence.test.ts`
passed clean (2 pass, 27 expect calls) across repeated runs.

### 6b. Addendum — governed-CI fix (branch `build/oa-07-ci-fix`, off `adoption-fixes-backlog @ dd748c9`)

The above (§1-6) is the historical record of the original `build/oa-07` work. This addendum
documents the follow-up fix for the governed-CI failure described in the new diff-summary
row at the top of §1.

Files touched (isolated worktree at `/workspace/wt-oa07ci`, no other builder's files
touched):
- `packages/substrate-local/src/pm-dispatch-fence.test.ts` — removed the fragile
  `expect(boardIds[0]).toBe(unlabeledId)` ordering precondition and its now-disproven
  "most-recently-touched" comment; added the new order-independent "OA-07 AC-7 CONTRAST"
  describe/test block (single-issue board positive control); removed the now-unused
  `readBoard` import.
- `packages/substrate-local/src/test-support/canned-pm-dispatch-fence.ts` — rewrote
  `readBoard`'s jsdoc to state the confirmed root cause (unsorted `readdirSync` order in
  ztrack's markdown backend) instead of the disproven "most-recently-touched" claim. No
  behavioral/logic change — `runDispatchFenceTick`'s pass-1/pass-2 logic is untouched.
- `docs/adoption-fixes/proofs/oa-07.md` (this file) — updated to describe the new,
  order-independent design; the HONEST BOUNDARY (§5) is unchanged (this fix is entirely
  about test determinism/non-tautology, not about the judgment-layer boundary it draws).

Explicitly NOT touched: no product code anywhere (`profiles/simple-sdlc/skills/pm/SKILL.md`,
`profiles/simple-sdlc/ir.yml`, `packages/substrate-local/src/emit.ts`,
`packages/substrate-local/src/runner-frontend.ts`); no OA-08 file; `adoption-fixes-backlog`
and `main` untouched (this work lives only on `build/oa-07-ci-fix`, landed onto
`adoption-fixes-backlog` by a separate lander after Fable review).

What was explicitly NOT done, per this task's hard prohibition: the expected value in the
removed assertion was NOT flipped to `"LOCAL-1"` — the assertion itself was removed, because
it was never a real invariant (an incidental, environment-dependent scan-order artifact).
The non-tautology protection was NOT weakened or deleted — it was relocated onto an
order-independent, single-issue-board positive control (the new CONTRAST test) that proves
the same thing (the fenced issue is a genuine, otherwise-dispatchable candidate) without
depending on an unverifiable cross-environment ordering claim.

Verified green on `build/oa-07-ci-fix`: `bun test packages/substrate-local/src/pm-dispatch-fence.test.ts`
passes clean (3 pass, 33 expect calls) across 10 repeated runs; confirmed order-independent
by temporarily reversing `readBoard`'s output (throwaway patch, reverted byte-identical) —
both orderings pass identically; `bun run check` (full repo gate) exits 0.
