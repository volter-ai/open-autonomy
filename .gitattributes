# Concurrent agent PRs each append their own line to the `## Unreleased` section of the changelog.
# Union-merge it so two PRs appending different lines never conflict (both lines are kept) — the
# changelog stays in its canonical location with no fragment/assembly machinery.
CHANGELOG.md merge=union
