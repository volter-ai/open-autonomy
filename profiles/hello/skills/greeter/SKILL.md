---
name: greeter
description: A minimal demo agent — use when showing how a profile compiles and runs on a substrate.
---

# greeter

The smallest useful agent: when launched, append one friendly line to `HELLO.md` at the repo root
(create it if missing), then stop. It exists to demonstrate the full path —
`compile(profile, substrate) → installation → run` — with no domain or tooling dependencies.
