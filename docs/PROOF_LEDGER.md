# Proof Ledger

This ledger maps every `.open-autonomy/roadmap.yml` proof gate to evidence.
Evidence may be live GitHub workflow proof, live bench workload issue proof, or a
deterministic CI fixture when model budget or external state would make a live
model run less reliable than the gate being tested.

| Proof Gate | Evidence | Status |
| --- | --- | --- |
| `retry-ci-failure` | `.codex/skills/pm/SKILL.md` (PM decides a failed PR from history — re-develop-with-context under `max_develop_attempts`, else escalate; no auto-loop), `docs/SPEC.md` (ci gates the merge) | done |
| `pm-open-pr-review` | `.codex/skills/pm/SKILL.md` (PM sweep + backpressure + launch routing) | done |
| `developer-context-review-fix` | `.codex/skills/developer/SKILL.md` (reads issue/CI/review context; escalates) | done |
| `head-changed-before-merge` | `docs/SPEC.md` (native auto-merge: required checks re-run on the current head) | done |
| `operator-pause-resume` | live `self-driving-conformance` run, issue [#5](https://github.com/volter-ai/open-autonomy-testbed/issues/5) (pause/status/develop-blocked/resume), Public Agent Session runs `27701483508` and `27704897971` | done |
| `five-issue-dogfood` | live `self-driving-conformance` run, issues #29-#33 → merged PRs #34-#38 with native auto-merge sessions `27701701974`, `27702036215`, and `27702212582`; canonical repo issue [#10](https://github.com/volter-ai/open-autonomy/issues/10) — first low-risk docs fix on canonical repo (fix incomplete sentence in `docs/OSS_AGENT_RUNBOOK.md`), all checks green (41 tests, 173 expectations, conformance pass, TypeScript clean) | done |
| `planner-creates-proof-gate-issues` | planner workflow runs `27648929065` and `27648929059`; planner tests | done |
| `scaffold-install-check` | `open-autonomy compile profiles/self-driving github <target>`, fleet preflight runs `27649190745` and `27649190743` | done |
| `quality-review-repair` | `.codex/skills/pm/SKILL.md` (PM feeds the reviewer's findings to a re-dispatched developer, or escalates — PM-directed, not an auto-repair loop), `.codex/skills/reviewer/SKILL.md` | done |
| `governance-maintainer-hold` | `docs/SPEC.md`, `.codex/skills/reviewer/SKILL.md` (block labels + the merge boundary) | done |
| `release-dogfood` | `VERSION`, `.open-autonomy/version.json`, `CHANGELOG.md`, `docs/OPERATIONS.md`, manifest version tests, and the committed release checklist in `docs/OPERATIONS.md` | done |
| `human-approve-merges-live` | live on the canonical repo: bot-authored PR #106 (human-required scope — `services/agent-model-proxy/wrangler.toml`) reached `done` only after a maintainer **Approve**, detected autonomously by the gate (`scripts/human-approval-gate.ts`) via the `pull_request_review` payload + repo-permission check, flipping `human-approval` to success so native auto-merge landed it (merge commit b5de40f). Runs `28152511853` (developer authored the PR) and `28153509570` (gate posted human-approval=success). The actor model + maintainer `kind:human` declaration is `docs/SPEC.md` + `profiles/self-driving/ir.yml`. | done |

## soc2-baseline control proofs (live, inspectable — this session, committed evidence)

Each row records COMMITTED inspectable evidence — private repo (`advanced_security: absent` where noted), run
ID, PR/status URL, and the log fact — not prose. The repos are kept live for independent audit.

| Control | Committed inspectable evidence | Status |
| --- | --- | --- |
| `soc2-PROFILE-DEFAULT-readiness` | **The SHIPPED profile's own defaults deliver this — no hand-config.** `profiles/soc2-baseline/provision.json:74-81` declares the 6 required checks by default; `:82` `enforce_admins:true`; `:83` `required_reviews:1`; `:85` `required_signatures:true`. Compiled to a build dir (`autonomy-compile … github`) → `provision.json` byte-**IDENTICAL** to the profile's (zero hand-edit) → provisioned a fresh repo `volter-ai/soc2-default-proof` STRAIGHT from the build dir (`provision-target-repo.ts`, **no `--manifest` override**, no console clicks). Read-back on a **private, GHAS-absent** (`advanced_security:null`) repo: `branches/main/protection` required contexts = exactly `[ci, agent-review, human-approval, supply-chain, code-scan, secret-scan]`, `enforce_admins=true`, reviews=1, ruleset `open-autonomy-required-signatures` **active**. Blocking control fires by default: command-injection PR [#2](https://github.com/volter-ai/soc2-default-proof/pull/2) → profile's `code-scan` gate run [`28364337936`](https://github.com/volter-ai/soc2-default-proof/actions/runs/28364337936) → **Semgrep "1 finding"** (`javascript.lang.security.detect-child-process` @ `src/handler.js:5`, ERROR) → `code-scan=failure` → PR `mergeStateStatus=BLOCKED`. **Causation ISOLATED (skeptic-panel finding closed):** drove the other 5 required checks GREEN on PR#2 head (`statusCheckRollup`: ci, agent-review, human-approval, secret-scan, supply-chain = SUCCESS) with ONLY `code-scan=FAILURE` → PR still `BLOCKED` (code-scan is *independently* load-bearing, not just overdetermined). **Not always-fail:** clean head `b51ea14` (injection removed) re-dispatched `code-scan` run [`28364937741`](https://github.com/volter-ai/soc2-default-proof/actions/runs/28364937741) → "Semgrep clean" → `code-scan=success`. | done |
| `soc2-C8-codescan-private` | `volter-ai/soc2-noghas-proof` (private, advanced_security=absent); planted command-injection PR [#2](https://github.com/volter-ai/soc2-noghas-proof/pull/2); code-scan run `28359520484` ran **Semgrep OSS** (env = GH_TOKEN+SHA only; no SEMGREP_APP_TOKEN, no GHAS) → `javascript.lang.security.detect-child-process` at **ERROR** severity → `code-scan=failure` → PR BLOCKED. Severity verified: the injection surfaces at `--severity=ERROR` in p/security-audit+p/javascript (1 ERROR finding). | done |
| `soc2-C12-secretscan-private` | `volter-ai/soc2-noghas-proof` (private, no GHAS); planted-AWS-secret PR [#1](https://github.com/volter-ai/soc2-noghas-proof/pull/1); secret-scan run `28359518693` ran **gitleaks** ("leaks found", exit 1) → `secret-scan=failure` → PR BLOCKED. | done |
| `soc2-C3-egress-private` | cell `volter-test-fixtures/bench-todo-cli-soc2-baseline-mqyo1ofl` (private, no GHAS); egress-guard run `28357621116`: 5728 GitHub `/meta` nets default-deny; `api.github.com -> 200`; `example.com curl exit=7 -> DENIED-ENFORCED`; reachability run confirmed proxy/npm/github reachable (loop intact). | done |
| `soc2-behaviorally-proven-6check` | cell; FUNDED loop on the CURRENT 6-check no-signup config: develop run `28361244003` → PR #12 (Verified head `481bbb9`) → all 6 required checks green (`ci, agent-review, human-approval, supply-chain, code-scan, secret-scan` — incl. the new no-signup gates) → squash-merged commit `6651b30f` (committer GitHub, **verified=true**). | done |
| `soc2-C6-signed-commits` | agent PR #12 head `481bbb9` verified=true; merge commit `6651b30f` verified=true. **HONEST CONSTRAINT:** `required_signatures` via a repository RULESET BLOCKS native auto-merge — verified live: an all-green + approved + Verified PR stays `mergeable_state=blocked` with the ruleset active (ruleset OR enforce_admins toggles don't unblock); classic `/protection/required_signatures` is **404 on private-no-GHAS**. So under `required_signatures` the merge needs an operator/admin action (or a momentary ruleset relax); the squash commit GitHub creates is Verified regardless. **ACCEPTED CONSTRAINT (recorded, by design):** with `required_signatures` ON, the final merge is an explicit human/operator step — signing is fully proven (commits Verified), the *merge-automation* is intentionally traded away for the stronger signing guarantee; documented in README C6 "Documented constraint", `control-matrix.md` C6, `provision.json:$comment`, `ir.yml` commit_signing comment. | done — accepted constraint |
| `soc2-C5-6checks` | `volter-ai/soc2-baseline-proof` branch-protection read-back = `[ci, agent-review, human-approval, supply-chain, code-scan, secret-scan]`, enforce_admins=true, reviews=1; required-signatures ruleset active. | done |
| `soc2-C7-supplychain-block` | `volter-ai/soc2-baseline-proof` PR #1 (github-sourced dep) → `supply-chain=failure` → BLOCKED. | done |
| `soc2-C9-depreview-block` | `volter-ai/soc2-depreview-test` PR #1 (lodash@4.17.4) → `dependency-review=failure` (10 vulns incl GHSA-jf85-cpcp-j695 critical) → BLOCKED (GHAS/public; dep CVEs also caught no-signup on private via the `supply-chain` `bun audit` gate). | done |
