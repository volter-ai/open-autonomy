# R11 closure

R11 closes the coding-harness worker runtime checkpoint. Launches bind canonical work, execution identity, authority, adapter protocol/implementation, session, fence, schemas, and content-addressed artifacts. Durable receipts, atomic effect ledgers, interprocess locks, stale-owner recovery, typed interactions, and result-specific evidence make retries and reconstruction deterministic. Executable bytes, filesystem access, network resolution, and argv/cwd authority are checked at the effect boundary inside a real process sandbox.

Closure is supported by two protocol-distinct live harnesses, a 17-test adversarial suite, and an independent skeptical review. Every R11 formal obligation is property-tested with zero residuals.
