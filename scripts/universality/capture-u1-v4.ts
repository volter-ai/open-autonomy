#!/usr/bin/env bun
import{createHash}from"node:crypto";import{mkdir}from"node:fs/promises";import{assertAcceptedGitHubSearch,censusTupleDigest,leafCountDigest,splitCreationRange,splitStarRange,type CensusRange,type CensusTuple}from"../../packages/core/src/organization-universality-census";import{verifySourceCensusContract,type FrozenSourceCensusContract}from"../../packages/core/src/organization-universality-census-contract";import{verifyFrozenUniversalityClaim,type FrozenUniversalityClaim}from"../../packages/core/src/organization-universality-claim";
const campaignId="organization-universality-2026-v4",cutoff="2026-07-20T23:59:59.999Z",root="docs/universality/campaign-v4/u1-github-raw",statePath=`${root}/capture-state.json`;await mkdir(root,{recursive:true});
type Attempt={attempt:number;startedAt:string;status:"running"|"failed"|"complete";failure?:string;completedAt?:string;tupleDigest?:string;leafCountDigest?:string;repositoryCount?:number;result?:string};type State={schema:"open-autonomy.github-census-state.v1";campaignId:string;registrationCommit:string;captureImplementationCommit:string;sourceCensusContractDigest:string;identityOwners:Record<string,string>;attempts:Attempt[];converged?:{firstAttempt:number;secondAttempt:number;completedAt:string;tupleDigest:string;leafCountDigest:string;population:string}};
const commit=(await Bun.$`git rev-parse 7c245e3^{commit}`.quiet().text()).trim(),implementationCommit=(await Bun.$`git rev-parse HEAD^{commit}`.quiet().text()).trim();if(Bun.spawnSync(["git","merge-base","--is-ancestor",commit,implementationCommit]).exitCode!==0)throw Error("preregistration commit is not an ancestor of capture implementation");for(const path of["scripts/universality/capture-u1-v4.ts","packages/core/src/organization-universality-census.ts","packages/core/src/organization-universality-census.test.ts"]){if(Bun.spawnSync(["git","diff","--quiet","HEAD","--",path]).exitCode!==0)throw Error(`capture implementation path is dirty: ${path}`)}const show=(path:string)=>{const result=Bun.spawnSync(["git","show",`${commit}:${path}`]);if(result.exitCode!==0)throw Error(`missing preregistered artifact ${path}`);return JSON.parse(new TextDecoder().decode(result.stdout))},contract=verifySourceCensusContract(show("docs/universality/campaign-v4/source-census-contract.json") as FrozenSourceCensusContract),claim=verifyFrozenUniversalityClaim(show("docs/universality/campaign-v4/claim.json") as FrozenUniversalityClaim);if(claim.sourceCensusContractDigest!==contract.digest||contract.campaignId!==campaignId||contract.censusCutoff!==cutoff)throw Error("preregistered census contract join mismatch");const initial:State={schema:"open-autonomy.github-census-state.v1",campaignId,registrationCommit:commit,captureImplementationCommit:implementationCommit,sourceCensusContractDigest:contract.digest,identityOwners:{},attempts:[]};let state:State=await Bun.file(statePath).exists()?await Bun.file(statePath).json():initial;state.identityOwners??={};
if(state.registrationCommit!==commit||state.captureImplementationCommit!==implementationCommit||state.sourceCensusContractDigest!==contract.digest||state.campaignId!==campaignId)throw Error("capture state provenance mismatch");for await(const relative of new Bun.Glob("attempt-*/*.headers.json").scan(root)){const headerPath=`${root}/${relative}`,header=await Bun.file(headerPath).json();if(header.status!==200)continue;const bodyPath=headerPath.replace(/\.headers\.json$/,".json.gz");if(!await Bun.file(bodyPath).exists())throw Error("retained accepted response body missing");const raw=Bun.gunzipSync(new Uint8Array(await Bun.file(bodyPath).arrayBuffer())),digest=`sha256:${createHash("sha256").update(raw).digest("hex")}`;if(digest!==header.bodyDigest)throw Error("raw-body-digest-mismatch");let value;try{value=assertAcceptedGitHubSearch(JSON.parse(new TextDecoder().decode(raw)))}catch{continue}if(value.incomplete_results)continue;for(const item of value.items){const name=item.full_name.toLowerCase(),owner=state.identityOwners[item.node_id];if(owner&&owner!==name)throw Error("node-id-alias");state.identityOwners[item.node_id]=name}}await Bun.write(statePath,JSON.stringify(state,null,2)+"\n");if(state.converged){console.log(JSON.stringify(state.converged,null,2));process.exit(0)}
const credential=Bun.spawnSync(["git","credential","fill"],{stdin:Buffer.from("protocol=https\nhost=github.com\n\n")});const password=new TextDecoder().decode(credential.stdout).split("\n").find(x=>x.startsWith("password="))?.slice(9);if(!password)throw Error("authenticated GitHub credential required for bounded exhaustive capture");
const save=()=>Bun.write(statePath,JSON.stringify(state,null,2)+"\n"),sleep=(ms:number)=>new Promise(resolve=>setTimeout(resolve,ms));class RetryPass extends Error{}
const rangeKey=(range:CensusRange)=>JSON.stringify(range),query=(range:CensusRange)=>[`stars:${range.stars[0]}..${range.stars[1]}`,"fork:true",range.created?`created:${range.created[0]}..${range.created[1]}`:null].filter(Boolean).join(" ");
async function runPass(attempt:Attempt){const dir=`${root}/attempt-${String(attempt.attempt).padStart(2,"0")}`;await mkdir(dir,{recursive:true});let sequence=0;const evidenceRefs:Array<{body:string;headers:string;url:string;digest:string;headerDigest:string}>=[];const request=async(q:string,perPage:number,page:number)=>{const url=`https://api.github.com/search/repositories?q=${encodeURIComponent(q)}&sort=stars&order=desc&per_page=${perPage}&page=${page}`;for(;;){const id=String(++sequence).padStart(5,"0"),startedAt=new Date().toISOString();let response:Response,bytes:Uint8Array;try{response=await fetch(url,{headers:{Accept:"application/vnd.github+json",Authorization:`Bearer ${password}`,"User-Agent":"open-autonomy-universality-census","X-GitHub-Api-Version":"2022-11-28"}});bytes=new Uint8Array(await response.arrayBuffer())}catch(error){await Bun.write(`${dir}/${id}.transport-error.json`,JSON.stringify({url,startedAt,failedAt:new Date().toISOString(),error:String(error)},null,2)+"\n");throw new RetryPass("transport-failure")}
    const observedAt=new Date().toISOString(),headers=Object.fromEntries(response.headers.entries()),digest=`sha256:${createHash("sha256").update(bytes).digest("hex")}`,body=`${dir}/${id}.json.gz`,headerPath=`${dir}/${id}.headers.json`;await Bun.write(body,Bun.gzipSync(bytes));await Bun.write(headerPath,JSON.stringify({url,startedAt,observedAt,status:response.status,headers,bodyDigest:digest},null,2)+"\n");
    const reset=Number(response.headers.get("x-ratelimit-reset")??0);if(response.status===403&&Number.isFinite(reset)&&reset>0){if(Date.now()>=Date.parse(cutoff))throw Error("accepted-response-at-or-after-cutoff");await sleep(Math.max(1000,Math.min(60_000,reset*1000-Date.now()+1000)));continue}
    if(response.status!==200)throw new RetryPass("non-rate-limit-non-200");if(Date.parse(observedAt)>=Date.parse(cutoff))throw Error("accepted-response-at-or-after-cutoff");if(!response.headers.get("content-type")?.toLowerCase().startsWith("application/json"))throw new RetryPass("malformed-response-schema");let parsed:unknown;try{parsed=JSON.parse(new TextDecoder().decode(bytes))}catch{throw new RetryPass("malformed-response-schema")}let value;try{value=assertAcceptedGitHubSearch(parsed)}catch{throw new RetryPass("malformed-response-schema")}if(value.incomplete_results)throw new RetryPass("incomplete-results-true");const stored=Bun.gunzipSync(new Uint8Array(await Bun.file(body).arrayBuffer())),storedDigest=`sha256:${createHash("sha256").update(stored).digest("hex")}`,headerBytes=new Uint8Array(await Bun.file(headerPath).arrayBuffer()),headerDigest=`sha256:${createHash("sha256").update(headerBytes).digest("hex")}`;if(storedDigest!==digest)throw Error("raw-body-digest-mismatch");evidenceRefs.push({body,headers:headerPath,url,digest,headerDigest});return value}};
  const passStartedAt=attempt.startedAt,rootResult=await request("stars:>=1000 fork:true",1,1),maximum=rootResult.items[0]?.stargazers_count;if(!Number.isSafeInteger(maximum)||maximum<1000)throw new RetryPass("malformed-response-schema");const queue:CensusRange[]=[{stars:[1000,maximum]}],leaves:Array<[string,number]>=[],repositories=new Map<string,any>(),names=new Map<string,string>();
  while(queue.length){const range=queue.shift()!,countResult=await request(query(range),1,1),count=countResult.total_count;if(count>1000){const[lo,hi]=range.stars;if(lo<hi){const[low,high]=splitStarRange(range.stars);queue.unshift({...range,stars:low},{...range,stars:high});continue}const dates=range.created??["2007-10-29",passStartedAt.slice(0,10)] as[string,string];if(dates[0]===dates[1])throw Error("unpartitionable-overflow-leaf");const[low,high]=splitCreationRange(dates);queue.unshift({...range,created:low},{...range,created:high});continue}
    const key=rangeKey(range);leaves.push([key,count]);for(let page=1;page<=Math.ceil(count/100);page++){const value=await request(query(range),100,page);if(value.total_count!==count)throw new RetryPass("leaf-total-count-drift");for(const item of value.items){if(item.stargazers_count<range.stars[0]||item.stargazers_count>range.stars[1]||repositories.has(item.node_id))throw new RetryPass("leaf-total-count-drift");if(range.created&&(!/^\d{4}-\d{2}-\d{2}T/.test(item.created_at)||item.created_at.slice(0,10)<range.created[0]||item.created_at.slice(0,10)>range.created[1]))throw new RetryPass("malformed-response-schema");const name=item.full_name.toLowerCase(),owner=state.identityOwners[item.node_id];if(owner&&owner!==name)throw Error("node-id-alias");state.identityOwners[item.node_id]=name;names.set(item.node_id,name);repositories.set(item.node_id,{repository:item.full_name,nodeId:item.node_id,stars:item.stargazers_count,defaultBranch:item.default_branch,license:item.license?.spdx_id??"NOASSERTION",fork:item.fork,archived:item.archived,createdAt:item.created_at,description:item.description,topics:item.topics??[],htmlUrl:item.html_url})}await save()}}
  const expected=leaves.reduce((sum,[,count])=>sum+count,0);if(repositories.size!==expected)throw new RetryPass("leaf-total-count-drift");for(const ref of evidenceRefs){const headerBytes=new Uint8Array(await Bun.file(ref.headers).arrayBuffer()),header=JSON.parse(new TextDecoder().decode(headerBytes)),stored=Bun.gunzipSync(new Uint8Array(await Bun.file(ref.body).arrayBuffer())),actual=`sha256:${createHash("sha256").update(stored).digest("hex")}`,actualHeader=`sha256:${createHash("sha256").update(headerBytes).digest("hex")}`;if(actual!==ref.digest||actualHeader!==ref.headerDigest||header.bodyDigest!==ref.digest||header.url!==ref.url||header.status!==200||Date.parse(header.observedAt)>=Date.parse(cutoff))throw Error("raw-body-digest-mismatch")}
  const rows=[...repositories.values()].sort((a,b)=>a.nodeId.localeCompare(b.nodeId)),tuples:CensusTuple[]=rows.map(r=>({node_id:r.nodeId,full_name:r.repository,stargazers_count:r.stars,fork:r.fork,archived:r.archived})),completedAt=new Date().toISOString();if(Date.parse(completedAt)>=Date.parse(cutoff))throw Error("no-two-pass-convergence-before-cutoff");const result=`${dir}/accepted-pass.json`,manifest=`${dir}/accepted-evidence-manifest.json`,tupleDigest=censusTupleDigest(tuples),countsDigest=leafCountDigest(leaves),manifestDigest=`sha256:${createHash("sha256").update(JSON.stringify(evidenceRefs)).digest("hex")}`,provenance={registrationCommit:commit,captureImplementationCommit:implementationCommit,sourceCensusContractDigest:contract.digest};await Bun.write(manifest,JSON.stringify({schema:"open-autonomy.github-census-evidence-manifest.v1",campaignId,...provenance,attempt:attempt.attempt,manifestDigest,requests:evidenceRefs},null,2)+"\n");await Bun.write(result,JSON.stringify({schema:"open-autonomy.github-census-pass.v1",campaignId,...provenance,attempt:attempt.attempt,startedAt:passStartedAt,completedAt,tupleDigest,leafCountDigest:countsDigest,evidenceManifest:manifest,evidenceManifestDigest:manifestDigest,leafCounts:Object.fromEntries(leaves),repositoryCount:rows.length,repositories:rows},null,2)+"\n");return{completedAt,result,tupleDigest,leafCountDigest:countsDigest,repositoryCount:rows.length}}
if(state.attempts.at(-1)?.status==="running"){state.attempts.at(-1)!.status="failed";state.attempts.at(-1)!.failure="transport-failure";await save()}
while(state.attempts.length<3&&!state.converged){const attempt:Attempt={attempt:state.attempts.length+1,startedAt:new Date().toISOString(),status:"running"};state.attempts.push(attempt);await save();try{Object.assign(attempt,await runPass(attempt),{status:"complete" as const});await save();const completed=state.attempts.filter(x=>x.status==="complete"),prior=completed.at(-2);if(prior&&prior.tupleDigest===attempt.tupleDigest&&prior.leafCountDigest===attempt.leafCountDigest){const publishedAt=new Date().toISOString();if(Date.parse(publishedAt)>=Date.parse(cutoff))throw Error("no-two-pass-convergence-before-cutoff");const population="docs/universality/campaign-v4/u1-github-enumeration.json",accepted=await Bun.file(attempt.result!).json();await Bun.write(population,JSON.stringify({schema:"open-autonomy.github-exhaustive-enumeration.v1",campaignId,registrationCommit:commit,captureImplementationCommit:implementationCommit,sourceCensusContractDigest:contract.digest,populationInstant:attempt.completedAt,publishedAt,censusCutoff:cutoff,firstConvergedAttempt:prior.attempt,secondConvergedAttempt:attempt.attempt,tupleDigest:attempt.tupleDigest,leafCountDigest:attempt.leafCountDigest,repositoryCount:accepted.repositoryCount,repositories:accepted.repositories},null,2)+"\n");state.converged={firstAttempt:prior.attempt,secondAttempt:attempt.attempt,completedAt:attempt.completedAt!,tupleDigest:attempt.tupleDigest!,leafCountDigest:attempt.leafCountDigest!,population};await save();break}}catch(error){attempt.status="failed";attempt.failure=String(error instanceof Error?error.message:error);await save();if(!(error instanceof RetryPass)){await Bun.write(`${root}/invalidation.json`,JSON.stringify({schema:"open-autonomy.github-census-invalidation.v1",campaignId,registrationCommit:commit,captureImplementationCommit:implementationCommit,sourceCensusContractDigest:contract.digest,reason:attempt.failure,attempts:state.attempts},null,2)+"\n");throw error}}console.log(JSON.stringify({attempt:attempt.attempt,status:attempt.status,failure:attempt.failure,repositories:attempt.repositoryCount,tupleDigest:attempt.tupleDigest}))}
if(!state.converged){await Bun.write(`${root}/invalidation.json`,JSON.stringify({schema:"open-autonomy.github-census-invalidation.v1",campaignId,registrationCommit:commit,captureImplementationCommit:implementationCommit,sourceCensusContractDigest:contract.digest,reason:"three-total-pass-attempts-exhausted",attempts:state.attempts},null,2)+"\n");throw Error("three-total-pass-attempts-exhausted")}
console.log(JSON.stringify(state.converged,null,2));
