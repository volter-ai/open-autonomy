name: Open Autonomy Governance Report

on:
  schedule:
    - cron: '43 6 * * 1'
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  report:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install --frozen-lockfile || bun install

      - name: Build decision index and report
        run: |
          mkdir -p .agent-run/governance
          bun scripts/public-agent-decision-index.ts \
            --out .agent-run/governance/decision-index.json
          bun scripts/open-autonomy-governance-report.ts \
            --index .agent-run/governance/decision-index.json \
            --out .agent-run/governance/report.json

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: open-autonomy-governance-${{ github.run_id }}
          path: .agent-run/governance
          if-no-files-found: warn
