# R4 independent compatibility closure

R4 is closed over the explicitly bounded portable subset. A clean-room Python author, isolated from the TypeScript source and tests, implemented structural checking, canonicalization and framed hashing, authored-default normalization, and experimental v1 migration from only public specifications, generated schemas, and examples. The exposure record and author feedback state both the inspected material and unsupported semantics.

The digest-locked corpus is executed through independent Bun/TypeScript and Python 3 processes. Seven observations compare canonical UTF-8 bytes, semantic hashes, diagnostic class/path projections, normalized documents, and migration documents plus loss accounting. The generated differential report has zero untriaged residuals. One wording/projection discrepancy is explicitly accepted as non-semantic; there are no normative mismatches.

Compatibility windows, discrepancy taxonomy, clean-room rules, and v1 migration limitations are published in `docs/ORGANIZATION-COMPATIBILITY-POLICY.md`. R4 does not claim full graph linking or whole-language semantic validation for the minimal Python utility.
